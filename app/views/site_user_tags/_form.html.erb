<%= form_for(@site_user_tag) do |f| %>
  <% if @site_user_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_user_tag.errors.count, "error") %> prohibited this site_user_tag from being saved:</h2>

      <ul>
      <% @site_user_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :site_user_id %><br>
    <%= f.text_field :site_user_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
