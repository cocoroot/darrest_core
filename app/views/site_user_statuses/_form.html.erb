<%= form_for(@site_user_status) do |f| %>
  <% if @site_user_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_user_status.errors.count, "error") %> prohibited this site_user_status from being saved:</h2>

      <ul>
      <% @site_user_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
