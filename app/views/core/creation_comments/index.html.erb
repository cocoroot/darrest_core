<p id="notice"><%= notice %></p>

<h1>Listing Core Creation Comments</h1>

<table>
  <thead>
    <tr>
      <th>Sid</th>
      <th>Creation</th>
      <th>User</th>
      <th>Parent</th>
      <th>Notice code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @core_creation_comments.each do |core_creation_comment| %>
      <tr>
        <td><%= core_creation_comment.sid %></td>
        <td><%= core_creation_comment.creation %></td>
        <td><%= core_creation_comment.user %></td>
        <td><%= core_creation_comment.parent %></td>
        <td><%= core_creation_comment.notice_code %></td>
        <td><%= link_to 'Show', core_creation_comment %></td>
        <td><%= link_to 'Edit', edit_core_creation_comment_path(core_creation_comment) %></td>
        <td><%= link_to 'Destroy', core_creation_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Creation comment', new_core_creation_comment_path %>
