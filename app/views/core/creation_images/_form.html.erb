<%= form_for(@core_creation_image) do |f| %>
  <% if @core_creation_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_creation_image.errors.count, "error") %> prohibited this core_creation_image from being saved:</h2>

      <ul>
      <% @core_creation_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :creation_id %><br>
    <%= f.text_field :creation_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
