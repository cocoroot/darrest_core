<p id="notice"><%= notice %></p>

<h1>Listing Core Creator Images</h1>

<table>
  <thead>
    <tr>
      <th>Creator</th>
      <th>Image</th>
      <th>In use</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @core_creator_images.each do |core_creator_image| %>
      <tr>
        <td><%= core_creator_image.creator %></td>
        <td><%= core_creator_image.image %></td>
        <td><%= core_creator_image.in_use %></td>
        <td><%= link_to 'Show', core_creator_image %></td>
        <td><%= link_to 'Edit', edit_core_creator_image_path(core_creator_image) %></td>
        <td><%= link_to 'Destroy', core_creator_image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Creator image', new_core_creator_image_path %>
