<%= form_for(@core_good) do |f| %>
  <% if @core_good.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_good.errors.count, "error") %> prohibited this core_good from being saved:</h2>

      <ul>
      <% @core_good.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :creation_id %><br>
    <%= f.text_field :creation_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
