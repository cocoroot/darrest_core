<%= form_for(@core_tag) do |f| %>
  <% if @core_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_tag.errors.count, "error") %> prohibited this core_tag from being saved:</h2>

      <ul>
      <% @core_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
