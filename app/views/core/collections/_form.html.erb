<%= form_for(@core_collection) do |f| %>
  <% if @core_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_collection.errors.count, "error") %> prohibited this core_collection from being saved:</h2>

      <ul>
      <% @core_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sid %><br>
    <%= f.text_area :sid %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
