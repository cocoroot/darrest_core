<p id="notice"><%= notice %></p>

<h1>Listing Core Report Images</h1>

<table>
  <thead>
    <tr>
      <th>Report</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @core_report_images.each do |core_report_image| %>
      <tr>
        <td><%= core_report_image.report %></td>
        <td><%= core_report_image.image %></td>
        <td><%= link_to 'Show', core_report_image %></td>
        <td><%= link_to 'Edit', edit_core_report_image_path(core_report_image) %></td>
        <td><%= link_to 'Destroy', core_report_image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Report image', new_core_report_image_path %>
