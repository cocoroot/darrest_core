<%= form_for(@core_category) do |f| %>
  <% if @core_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_category.errors.count, "error") %> prohibited this core_category from being saved:</h2>

      <ul>
      <% @core_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br>
    <%= f.text_field :parent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
