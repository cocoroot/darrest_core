<%= form_for(@creation_category) do |f| %>
  <% if @creation_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creation_category.errors.count, "error") %> prohibited this creation_category from being saved:</h2>

      <ul>
      <% @creation_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :creation_id %><br>
    <%= f.text_field :creation_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
