<%= form_for(@manage_collection) do |f| %>
  <% if @manage_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_collection.errors.count, "error") %> prohibited this manage_collection from being saved:</h2>

      <ul>
      <% @manage_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sid %><br>
    <%= f.text_area :sid %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
