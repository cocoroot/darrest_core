<%= form_for(@manage_creator) do |f| %>
  <% if @manage_creator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_creator.errors.count, "error") %> prohibited this manage_creator from being saved:</h2>

      <ul>
      <% @manage_creator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sid %><br>
    <%= f.text_field :sid %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
