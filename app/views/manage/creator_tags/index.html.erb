<p id="notice"><%= notice %></p>

<h1>Listing Manage Creator Tags</h1>

<table>
  <thead>
    <tr>
      <th>Creator</th>
      <th>Tag</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @manage_creator_tags.each do |manage_creator_tag| %>
      <tr>
        <td><%= manage_creator_tag.creator %></td>
        <td><%= manage_creator_tag.tag %></td>
        <td><%= link_to 'Show', manage_creator_tag %></td>
        <td><%= link_to 'Edit', edit_manage_creator_tag_path(manage_creator_tag) %></td>
        <td><%= link_to 'Destroy', manage_creator_tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Creator tag', new_manage_creator_tag_path %>
