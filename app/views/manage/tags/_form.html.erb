<%= form_for(@manage_tag) do |f| %>
  <% if @manage_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_tag.errors.count, "error") %> prohibited this manage_tag from being saved:</h2>

      <ul>
      <% @manage_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
