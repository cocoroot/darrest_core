<%= form_for(@manage_report_image) do |f| %>
  <% if @manage_report_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_report_image.errors.count, "error") %> prohibited this manage_report_image from being saved:</h2>

      <ul>
      <% @manage_report_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :report_id %><br>
    <%= f.text_field :report_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
